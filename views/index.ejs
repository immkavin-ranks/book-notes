<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Notes</title>
    <link rel="stylesheet" href="stylesheets/main.css" />
  </head>
  <body>
    <h1>Book Notes</h1>
    <h1 class="text-3xl font-bold underline">Hello world!</h1>
    <% for (var i = 0; i < books.length; i++) { %>
    <h2>
      <a href="/<%= books[i].title.replace(/[^a-zA-Z0-9]+/g, '') %>">
        <%= books[i].title %>
      </a>
    </h2>
    <img
      src="https://covers.openlibrary.org/b/isbn/<%= books[i].isbn %>-M.jpg"
    />
    <p><%= books[i].author %></p>
    <p><%= books[i].isbn %></p>
    <p><%= books[i].rating %></p>
    <p><%= books[i].review %></p>
    <% } %>
  </body>
</html>
