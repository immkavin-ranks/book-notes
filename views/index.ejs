<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Notes</title>
  </head>
  <body>
    <h1>Book Notes</h1>
    <% for (var i = 0; i < books.length; i++) { %>
    <h2>
      <a href="/book/<%= books[i].title.replace(/[^a-zA-Z0-9]+/g, '') %>">
        <%= books[i].title %>
      </a>
    </h2>
    <p><%= books[i].author %></p>
    <p><%= books[i].isbn %></p>
    <p><%= books[i].rating %></p>
    <p><%= books[i].review %></p>
    <% } %>
  </body>
</html>
